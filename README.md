# Custom-Shell
Simple to use UNIX shell developed using the C programming lanuage
--------------------------------------------------------------------
User Manual:
1. Compile this shell on your UNIX terminal of choice (Ubuntu/Linux preferred):
gcc myshell.c -o customshell

2. Run the shell with the command
./customshell

3. To find the list of built-in commands of the shell enter:
$help 
